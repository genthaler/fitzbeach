{
  "name": "fitzbeach",
  "private": true,
  "workspaces": [
    "aws",
    "backend",
    "frontend"
  ],
  "scripts": {
    "predev": "npm run -w backend codegen",
    "dev": "concurrently --kill-others-on-fail --names backend,frontend \"npm run -w backend watch\" \"npm run -w frontend dev\"",
    "verify": "npm run -w backend codegen && npm run -w frontend verify && npm run -w backend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "shx": "^0.4.0"
  }
}
