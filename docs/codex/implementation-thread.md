Use this repository's `AGENTS.md` and the relevant local skills for the requested implementation task.
Implement the change directly with minimal prompt overhead.
Update tests, ElmBook, and README only when the change makes them inaccurate.
Run the required verification and commit the result.
