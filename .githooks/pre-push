#!/usr/bin/env bash

set -euo pipefail

while read -r local_ref _ remote_ref _
do
  if [[ "$remote_ref" == "refs/heads/main" ]]; then
    echo "Direct pushes to main are blocked in this repo. Open a PR instead." >&2
    exit 1
  fi
done

exit 0
